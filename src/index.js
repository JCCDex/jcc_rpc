const JcBiz = require('./biz');
const JcConfig = require('./config');
const JcExchange = require('./exchange');
const JcInfo = require('./info');

exports = module.exports = {
    JcBiz,
    JcConfig,
    JcExchange,
    JcInfo
}